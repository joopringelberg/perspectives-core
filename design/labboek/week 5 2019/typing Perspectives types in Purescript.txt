TYPEN VOOR PERSPECTIVES BINNEN PURESCRIPT
Het zou mooi zijn als de Purescript compiler meer steun geeft bij de compositie van functies waarvan de parameters en resultaten Perspectives typen zijn. Dit is een collectie types die we zouden kunnen representeren:

	* Context
	* Rol
	* Property-waarde (Val)
	* BuitenRol
	* BinnenRol (??)
	* SimpleValue
	* PBool
	* PString
	* PDate
	* PNumber
	
	* Context-definitie
	* Rol-definitie
	* Property-definitie
	
Maar op dat niveau kunnen we  natuurlijk verder gaan:
	* View
	* SysteemBot
	* Actie
	* Zaak
	* enzovoort.

Waar houden we op?

We kunnen gebruik maken van newtype:

newtype Context = Context String
newtype Rol = Rol String
newtype Val = Val String
newtype PBool = PBool String -- type level strings?
newtype PString = PString String

newtype ContextDef = ContextDef String
newtype RolDef = RolDef String
newtype PropertyDef = PropertyDef String

VOORBEELDEN VAN FUNCTIES

UIT Perspectives.DataTypeObjectGetters (allemaal ObjectGetters)

type MP e a = MonadPerspectives (AjaxAvarCache e) (Array a)

contextType :: forall e. Context -> MP e ContextDef

buitenRol :: forall e. Context -> MP e BuitenRol

iedereRolInContext :: forall e. Context -> MP e Rol

typeVanIedereRolInContext :: forall e. Context -> MP e RolDef

propertyTypen :: forall e. Rol -> MP e PropertyDef

internePropertyTypen :: forall e. Rol -> MP e PropertyDef

label :: forall e. Context -> MP e Val

rolType :: forall e. Rol -> MP e RolDef

rolBindingDef :: forall e. Rol -> MP e Context -- MERK OP: de naam is misleidend, waarschijnlijk heb ik daar een denkfout gemaakt...

context :: forall e. Rol -> Context



UIT Perspectives.ModelBasedObjectGetters

rolIsVerplicht :: forall e. RolDef -> MP e PBool

rolIsFunctioneel :: forall e. RolDef -> MP e PBool

range :: forall e. RolDef -> MP e SimpleValue

buitenRolBeschrijving :: forall e. ContextDef -> RolDef

binnenRolBeschrijving :: forall e. ContextDef -> RolDef

rolInContextContextDef :: forall e. RolDef -> ContextDef



PHANTOM TYPES
newtype PST a = PST String

data PerspectivesTypes = Context | Rol | Value ...

type Context = PST Context