GebruikerGegevens

import model:Test als tst:

tst:TestCaseInCouchdb usr:MyTestCase
	$Self -> usr:MijnSysteem$User(1)
	$ARole => ()
	$NestedContext3 =>
		$SubCase3 usr:MySubcase
			$Self -> usr:MijnSysteem$User(1)
			$RoleToInspect => ()
